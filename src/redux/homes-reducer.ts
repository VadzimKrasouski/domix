const initialState: any = {}

export const homesReducer = (state: any = initialState, action: any): any => {
    // @ts-ignore
    switch (action.type) {
        default:
            return state;
    }
};